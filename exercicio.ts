function multiplica(numeroUm: number, numeroDois: number) {
    return numeroUm * numeroDois;
}

const resultado = multiplica(3, 3);
console.log(resultado);

function dizNome(nome: string) {
    return `Olá ${nome}`
}

const apresentacao = dizNome("Nicacio");
console.log(apresentacao);